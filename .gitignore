.DS_Store




.claude/
.gemini/
docs/superpower/
docs/maestro/
.planning/
GEMINI.md
CLAUDE.md
AGENTS.md

# Local/CI build output (anchored at repo root — does NOT match the tracked,
# embedded server/dist). /shoka is the build.sh artifact; /dist is the release
# build dir used by nfpm + the release workflow.
/shoka
/dist/

